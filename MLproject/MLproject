name: latihan-cs-mlflow

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      dataset: {type: string, default: "titanic_data_preprocessed.csv"}
      solver: {type: string, default: "liblinear"}
    command: >
      python modelling.py {dataset} {solver}
